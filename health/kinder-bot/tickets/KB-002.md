improve directory structure (put Python sources into a @src folder with package directory)
optimize @Dockerfile so that it installs requirements on an upper layer before copying
the python source and @.kinder-bot.pigovsky.com directories
