add a @push.sh script and update @docker-compose.yml 
that will build the docker image `pigovsky/kinder-bot.pigovsky.com`
tagging it with the version stated in @.kinder-bot.pigovsky.com/VERSION
and pushing it to the docker hub.
